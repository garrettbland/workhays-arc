import * as TemplateModule from '../index'

describe('definePartials', () => {
    it.todo('Should compile and define partials from array of partials')
})

describe('renderWithLayout', () => {
    it.todo('Should render content with template')
    it.todo('Should render content with default template')
})
